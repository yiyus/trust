 Eval←{                                    ⍝ Get result and Jacobian matrix of ⍺⍺ at ⍵
    (1<|≡g)∧1<≢g←⍺⍺ ⍵:g ⋄ ⍺←⎕CT*÷2         ⍝     pass jacobian if given, else ⍺ is perturbation
    (⊃⊆g)(⍉↑⍺÷⍨(⍺⍺¨(⊂⍵)+↓↑(-⍳≢⍵)↑¨⍺)-⊆g)   ⍝     finite-differences method
 }
